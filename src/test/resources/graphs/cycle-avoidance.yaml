description: >
  A cycle exists in the graph. The walk should detect it and
  find the path through the non-cyclic branch.

roots:
  - type: JavaFrame
    objects: [root]

objects:
  root:
    class: com.example.Root
    fields:
      entry: node_a
  node_a:
    class: com.example.NodeA
    fields:
      next: node_b
      project: target1
  node_b:
    class: com.example.NodeB
    fields:
      next: node_a

targets: [target1]
target_class: com.example.ProjectImpl

expected_paths:
  - "Root[JavaFrame] -> com.example.Root.entry -> com.example.NodeA.project -> com.example.ProjectImpl"
